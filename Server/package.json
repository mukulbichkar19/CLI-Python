{
  "name": "server",
  "version": "1.0.0",
  "description": "This is an api server for adding new email subscribers and checking if any same entry exists",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "author": "mukul",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^1.18.10"
  },
  "dependencies": {
    "bluebird": "^3.5.3",
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "mongoose": "^5.4.15"
  }
}
